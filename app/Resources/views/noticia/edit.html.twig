{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Edicion de la Noticia</h1>

	<form action="{{ path('noticia_edit', { 'id': noticium.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    	<div class="row top-buffer">
    	<input type="hidden" name="_method" value="POST" />
    	{{ form_widget(edit_form) }}
        </div>
        <div class="row top-buffer">
            <div class="span3">
                <button type="submit" class="btn btn-large btn-block btn-success">Guardar Cambios</a>
            </div>
            <div class="span3">
                <a href="{{ path('noticia_index') }}" class="btn btn-large btn-block btn-default">Volver a Noticias</a>
            </div>
            
    </form>
    <div class="span3">
                <form action="{{ path('noticia_delete', { 'id': noticium.id }) }}" method="post">
                    <input type="hidden" name="_method" value="DELETE" />
                    {{ form_widget(delete_form) }}
                    <button type="submit" class="btn btn-large btn-block btn-warning">Borrar</button>
                </form>
            </div>
        </div>

{% endblock %}